EQUIVS = -Dsdot=ddot -Dsasum=dasum -Dsaxpy=daxpy -Dsscal=dscal -Dsnrm2=dnrm2 -Dscopy=dcopy -Dsgbtrf=dgbtrf -Dsgbtrs=dgbtrs -Dsgetri=dgetri -Dsgetrf=dgetrf -DPERF_TIMER -D_OPENMP

CPP = /lib/cpp -C -traditional
CPPOPTS = -C -P -I${srcdir} ${EQUIVS}
SED = sed s:\#PREC\#:d:g
LINK_MOD = 

#FC = pgf90
FC = mpif90
ifdef SOLPS_DEBUG
FCOPTS = -Mbackslash -C -g -Mchkfpstk -Mchkptr -Mchkstk -Mpgicoff
else
#FCOPTS = -O2 -Mbackslash
#FCOPTS = -fast -Mbackslash
FCOPTS = -fast -Mbackslash -mp=nonuma
endif

DBLFC=${FC}
DBLOPTION=
DBLSED = sed -e s:\#PREC\#:d:g

INCMOD = -I
MOD = mod

BLD = ar rcv

GRLIBES = ${NCARG} -lmscl
LDLIBES = -L${SOLPS_LIB} ${NAG} -lnetcdf
LDLIBES = -L${SOLPS_LIB} ${NAG} -L /afs/efda-itm.eu/project/switm/netcdf/amd64_pgi/lib -lnetcdff -lnetcdf

#LD = pgf90
LD = mpif90
ifdef SOLPS_DEBUG
LDOPTS = -g -C
else
LDOPTS = -mp=nonuma
endif
