EQUIVS = -DNO_CDF

CPP = /usr/lib/gpp -F
CPPOPTS = -C -P -N -I$(srcdir) $(EQUIVS)

FC = f90
FCOPTS = -Oscalar2,vector3,task1 -ei -r2 -Wr"-v 200" -ef
INCMOD = -p
MOD = o

BLD = bld rucv

GRLIBES = $(NCARG)
LDLIBES = $(NAG)
ifdef PERFMON
LDLIBES += -lperf
endif

LD = f90
LDOPTS = -Wl"-M,s"

