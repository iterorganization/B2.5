      subroutine F01AAF(A, IA, N, X, IX, WKSPCE, IFAIL) 
      INTEGER IA, N, IX, IFAIL 
      real*8                                               !sv
     *	A(IA,N), X(IX,N), WKSPCE(N)                      !sv 
      write(*,*) 'NAG routine called and NO_NAG set'
      stop 'ERROR'
      end
