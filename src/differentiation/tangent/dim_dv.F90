!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!  Differentiation of dim in forward (tangent) mode (with options multiDirectional context noISIZE r8):
!   variations   of useful results: dim
!   with respect to varying inputs: x y
SUBROUTINE DIM_DV(x, xd, y, yd, dim, dimd, nbdirs)
  USE B2MOD_TYPES
!  Hint: nbdirsmax should be the maximum number of differentiation directions
  USE B2MOD_DIFFSIZES
  IMPLICIT NONE
  REAL(kind=r8) :: x, y, dum
  REAL(kind=r8), DIMENSION(nbdirsmax), INTENT(OUT) :: dimd, xd, yd
  INTRINSIC MAX
  INTEGER :: nd
  REAL(kind=r8), INTENT(OUT) :: dim
  INTEGER :: nbdirs
  IF (x - y .LT. 0.0_R8) THEN
    dim = 0.0_R8
    DO nd=1,nbdirsmax
      dimd(nd) = 0.D0
    END DO
  ELSE
    DO nd=1,nbdirs
      dimd(nd) = xd(nd) - yd(nd)
    END DO
    dim = x - y
  END IF
!
  RETURN
END SUBROUTINE DIM_DV
!

