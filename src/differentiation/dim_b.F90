!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.15 (feature/bugFixes) - 10 Jul 2020 12:08
!
!  Differentiation of dim in reverse (adjoint) mode:
!   gradient     of useful results: x y dim
!   with respect to varying inputs: x y
!   RW status of diff variables: x:incr y:incr dim:in-killed
SUBROUTINE DIM_B(x, xb, y, yb, dimb)
  USE B2MOD_TYPES
  IMPLICIT NONE
!     ------------------------------------------------------------------
  REAL(kind=r8) :: dim, x, y
  REAL(kind=r8) :: dimb, xb, yb
!     ------------------------------------------------------------------
!      B2.5 representation of intrinsic function dim which is 
!      needed for differentiation with TAPENADE       
!     ------------------------------------------------------------------
  REAL(kind=r8) :: dummy
  REAL(kind=r8) :: dummyb
!
  dummy = x - y
  IF (dummy .GE. 0.0_R8) THEN
    dummyb = dimb
  ELSE
    dummyb = 0.0
  END IF
  xb = xb + dummyb
  yb = yb - dummyb
END SUBROUTINE DIM_B
!

