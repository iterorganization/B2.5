!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (feature_llhTests) - 27 May 2021 14:23
!
!
!
!
!
!
!
!
!
!
!
!
MODULE B2MOD_NEOCLASSICAL_DIFF
  USE B2MOD_TYPES
  USE B2MOD_B2CMPA_DIFF
  USE B2MOD_VERSION_DIFF
  IMPLICIT NONE
!
!
!     max. number of ionisation stages
  INTEGER, SAVE :: nzdecl
!
  LOGICAL, SAVE :: newneo
  INTEGER, SAVE :: nsl
  INTEGER, ALLOCATABLE, SAVE :: ncl(:), isort(:, :)
  REAL(kind=r8), ALLOCATABLE, SAVE :: m(:), zsp(:, :), theta(:, :), bav_&
& (:), b2av_(:), bi2a_(:), rbt_(:), bgradp_(:), dpsidr_(:), rnq_(:), fc_&
& (:), gclass_(:), fm_(:, :), r2i_(:), rho_(:)
  REAL(kind=r8), ALLOCATABLE, SAVE :: dd(:, :, :), vvn(:, :, :), chi(:, &
& :, :), vwt(:, :, :)

CONTAINS
!
!
  SUBROUTINE ALLOC_B2MOD_NEOCLASSICAL(nxd, nyd, nsd, mmxd)
    IMPLICIT NONE
!
    INTEGER :: nxd, nyd, nsd, nzd, mmxd
    INTRINSIC MAXVAL
    INTRINSIC NINT
    REAL(kind=r8) :: result1
!
    result1 = MAXVAL(zn(0:nsd-1))
    nzdecl = NINT(result1) + 1
    nzd = nzdecl
    ALLOCATE(ncl(0:nsd))
    ALLOCATE(isort(0:nsd, 0:nzd-1))
    ALLOCATE(m(0:nsd))
    ALLOCATE(zsp(0:nsd, 0:nzd-1))
    ALLOCATE(theta(-1:nxd, -1:nyd))
    ALLOCATE(bav_(-1:nyd))
    ALLOCATE(b2av_(-1:nyd))
    ALLOCATE(bi2a_(-1:nyd))
    ALLOCATE(rbt_(-1:nyd))
    ALLOCATE(bgradp_(-1:nyd))
    ALLOCATE(dpsidr_(-1:nyd))
    ALLOCATE(rnq_(-1:nyd))
    ALLOCATE(fc_(-1:nyd))
    ALLOCATE(gclass_(-1:nyd))
    ALLOCATE(fm_(1:mmxd, -1:nyd))
    ALLOCATE(r2i_(-1:nyd))
    ALLOCATE(rho_(-1:nyd))
    ALLOCATE(dd(0:nsd, 0:nzd-1, -1:nyd))
    ALLOCATE(vvn(0:nsd, 0:nzd-1, -1:nyd))
    ALLOCATE(chi(0:nsd, 0:nzd-1, -1:nyd))
    ALLOCATE(vwt(0:nsd, 0:nzd-1, -1:nyd))
  END SUBROUTINE ALLOC_B2MOD_NEOCLASSICAL

!
  SUBROUTINE DEALLOC_B2MOD_NEOCLASSICAL()
    IMPLICIT NONE
    INTRINSIC ALLOCATED
!
    IF (.NOT.ALLOCATED(ncl)) THEN
      RETURN
    ELSE
      DEALLOCATE(ncl)
      DEALLOCATE(isort)
      DEALLOCATE(m)
      DEALLOCATE(zsp)
      DEALLOCATE(theta)
      DEALLOCATE(bav_)
      DEALLOCATE(b2av_)
      DEALLOCATE(bi2a_)
      DEALLOCATE(rbt_)
      DEALLOCATE(bgradp_)
      DEALLOCATE(dpsidr_)
      DEALLOCATE(rnq_)
      DEALLOCATE(fc_)
      DEALLOCATE(gclass_)
      DEALLOCATE(fm_)
      DEALLOCATE(r2i_)
      DEALLOCATE(rho_)
      DEALLOCATE(dd)
      DEALLOCATE(vvn)
      DEALLOCATE(chi)
      DEALLOCATE(vwt)
!
      RETURN
    END IF
  END SUBROUTINE DEALLOC_B2MOD_NEOCLASSICAL

END MODULE B2MOD_NEOCLASSICAL_DIFF
!
!

